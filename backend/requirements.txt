fastapi==0.115.0
uvicorn[standard]==0.30.6
duckdb==1.1.0
anthropic==0.109.1
httpx==0.28.1       # pinned with anthropic 0.109; the old <0.28 cap was for anthropic 0.34's proxies= usage (gone since). Pinned to stop transitive drift from re-breaking the chat ("Ask the data") path on redeploy.
python-dotenv==1.0.1
python-multipart==0.0.9
